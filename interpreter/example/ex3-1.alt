process iteration(var n: int) {
    var i = 1;
    var somme = 0;

    while (i <= n) {
        somme += i;
        i++;
    }
    printf("La somme des entiers de 0 à %d est %d\n", n, somme);
}

/*
process iteration_with_for(var n:int) {
    var somme = 0;
    for (i in 1..(n+1)) {
        somme += i;
    }
    printf("La somme des entiers de 0 à %d est %d\n", n, somme);
}
*/

main {
    run iteration(5);
}